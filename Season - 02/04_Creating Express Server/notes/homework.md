// Create a Repo
// Initialize a Repository
// read about node_modules , package.json, and package-lock.json
// Install express
// Create a basic express server that listens on port 3000
// Write request handlers for two routes test and hello
// Install nodemon as a dev dependency or globally and update the start script to use nodemon in package.json
// What are dependencies and devDependencies?
// What is the meaining of -g while installing a package?
// difference between caret and tilde in package.json versioning?